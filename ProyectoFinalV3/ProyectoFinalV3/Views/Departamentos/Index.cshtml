@model IEnumerable<ProyectoFinalV3.Models.Departamento>

@{
    ViewBag.Title = "Index";
}

<h2>Departamentos</h2>

<p>
    @Html.ActionLink("Crear nuevo Departamento", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Departamento1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Departamento1)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.Codigo_Departamento }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.Codigo_Departamento })
        </td>
    </tr>
}

</table>
