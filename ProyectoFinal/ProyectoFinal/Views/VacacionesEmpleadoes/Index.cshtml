@model IEnumerable<ProyectoFinal.Models.VacacionesEmpleado>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FechaDesde)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FechaHasta)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CorrespondienteAño)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ComentarioVacacion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MantenimientoEmpleado.CodigoEmpleado)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FechaDesde)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FechaHasta)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CorrespondienteAño)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ComentarioVacacion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MantenimientoEmpleado.CodigoEmpleado)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
